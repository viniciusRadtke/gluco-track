import { PagePlaceholder } from '@/components/page-placeholder'

export function HistoryPage() {
  return (
    <PagePlaceholder description="Lista de todos os registros, com filtro por período e por tipo." />
  )
}
